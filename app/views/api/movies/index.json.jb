@movies.map do |movie|
  {
    id: movie.id,
    title: movie.title,
    year: movie.year,
    plot: movie.plot,
    image: movie.image,
    english: movie.english,

    genres: movie.genres,
  }
end
